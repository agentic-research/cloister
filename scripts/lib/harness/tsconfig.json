{
  "//": "Standalone type-check for the harness launch pipeline. `scripts/` is NOT in the root tsconfig (which covers src/**/*.ts only) and has no checkJs anywhere, so JSDoc under scripts/ is unchecked convention — decoration that reads like a guarantee. This turns it on for this directory so the pipeline's types are enforced rather than documented. Same shape and same reason as tools/harness-shim/tsconfig.json + lint:shim. Run via `task lint:harness-types`.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "allowJs": true,
    "checkJs": true,
    "strict": true,
    "noEmit": true,
    "skipLibCheck": true,
    "types": []
  },
  "include": ["*.mjs"]
}
